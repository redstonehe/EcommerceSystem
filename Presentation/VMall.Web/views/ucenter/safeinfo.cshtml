@model VMall.Core.PartUserInfo
@{
    Layout = "~/views/shared/_layout_user.cshtml";
    ViewBag.Title = "账户安全-" + WorkContext.MallConfig.SiteTitle;
    ViewBag.PositionTitle = "账户安全";
    ViewBag.Mark = 2;
    ViewBag.IsBox1210 = 0;
}
<style>
    .width50 {width:50px;
    }
    .safe .submitBT {
    position: absolute;
    right: 0px;
    padding: 0px 10px;
    top: 10px;
    width: 50px;
    text-align: center;
        color: #fff;
}
</style>
<div class="bigBox" id="member">
    <div class="box">
        @Html.Partial("/views/ucenter/leftNav.cshtml")

        <div id="memberR">
            <h2 id="memberRT">@ViewBag.PositionTitle</h2>

            <div class="safe">
                <s class="ico1"></s>
                <b>登录密码</b>
                <p>互联网账号存在被盗风险，建议您定期更改密码以保护账户安全。</p>
                <a href="@Url.Action("safeverify", new RouteValueDictionary { { "act", "updatePassword" } })" class="submitBT width50">修改</a>
                <div class="clear"></div>
            </div>

            <div class="safe">
                <s class="@if (Model.VerifyEmail == 1)
                          {<text>ico1</text>}
                          else
                          {<text>ico2</text>}"></s>
                <b>邮箱验证</b>
                <p>@if (Model.VerifyEmail == 0)
                   {<text>建议您绑定并验证邮箱以保护账户安全</text>}
                   else
                   {<text>邮箱已验证，可用于快速找回登录密码。</text>}</p>
                <a href="@Url.Action("safeverify", new RouteValueDictionary { { "act", "updateEmail" } })" class="submitBT width50">
                    @if (Model.Email.Length > 0)
                    {
                        if (Model.VerifyEmail == 0)
                        {
                        @:验证
                }
                        else
                        {
                        @:修改
                }
                    }
                    else
                    {
                        @:绑定
            }
                </a>
                <div class="clear"></div>
            </div>

            <div class="safe">
                <s class="@if (Model.VerifyMobile == 1)
                          {<text>ico1</text>}
                          else
                          {<text>ico2</text>}"></s>
                <b>手机验证</b>
                <p>@if (Model.VerifyMobile == 0)
                   {<text>建议您绑定并验证手机以保护账户安全</text>}
                   else
                   {<text>您验证的手机：<strong>@Html.Raw(string.IsNullOrEmpty(Model.Mobile) ? "" : CommonHelper.HideMobile(Model.Mobile))</strong> 若已丢失或停用，请立即更换，<span style="color: #cc0000;">避免账户被盗</span></text>}</p>
                <a href="@Url.Action("safeverify", new RouteValueDictionary { { "act", "updateMobile" } })" class="submitBT width50">
                    @if (Model.Mobile.Length > 0)
                    {
                        if (Model.VerifyMobile == 0)
                        {
                        @:验证
                }
                        else
                        {
                        @:修改
                }
                    }
                    else
                    {
                        @:绑定
            }
                </a>
                <div class="clear"></div>
            </div>

            <div class="safe">
                <s class="@if (!string.IsNullOrEmpty(Model.PayPassword))
                          {<text>ico1</text>}
                          else
                          {<text>ico2</text>}"></s>
                <b>支付密码</b>
                <p>支付密码用于资产支付等敏感操作，请妥善保管，建议您定期更改密码以保护账户安全。</p>
                <a href="@Url.Action("safeverify", new RouteValueDictionary { { "act", "updatepaypassword" } })" class="submitBT width50">
                    @if (!string.IsNullOrEmpty(Model.PayPassword)){
                        @:修改
                        }
                    else{
                        @: 设置
                    }
                </a>
                <div class="clear"></div>
            </div>

            <div class="safeInfo">
                <h2>安全服务提示</h2>
                <p>1.确认您登录的是seedm1网址@*<a href="#">@WorkContext.MallConfig.SiteUrl</a>*@，注意防范进入钓鱼网站，不要轻信各种即时通讯工具发送的商品或支付链接，谨防网购诈骗。 </p>
                <p>2.建议您安装杀毒软件，并定期更新操作系统等软件补丁，确保账户及交易安全。 </p>
            </div>

        </div>

        <div class="clear"></div>
    </div>
    <div class="clear"></div>
</div>

